export function DayActivities(props: {titles: string[], details: string[]}) {
    return (
        <div>

        </div>
    )
}

function renderActivity(title: string, details: string, ) {
    return(
        <div>ACTIVITY</div>
    )
}